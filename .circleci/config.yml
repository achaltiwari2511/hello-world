version: 2.1
orbs:
  node: circleci/node@4.7.0
  jobs:
    build:
      executor:
        name: node/default
          tag: '10.4'
        steps:
          -cheakout
          -node/with-cache:
          steps:
            -run: npm install
        -run:npm run rest
